import { Widget } from '@typeform/embed-react'

const Typeform = () => {
  return <Widget style={{
    height: '80vh',
    width: '100%',
    margin: 'auto auto 3em auto',
  }} id="jdnSVW0u" className="my-form" />
}

export default Typeform
